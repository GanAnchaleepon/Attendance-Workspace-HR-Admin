-- ============================================================================
-- Migration: เพิ่มคอลัมน์ sort_order ให้ตาราง employees
-- ใช้เก็บลำดับเดิมตามไฟล์ Manpower/Manpower QA เพื่อให้หน้า "ตรวจสอบ OT" เรียงลำดับ
-- พนักงานตามไฟล์ Manpower ได้ถูกต้อง
--
-- รันไฟล์นี้ผ่าน phpMyAdmin ถ้าฐานข้อมูลของคุณสร้างจาก schema.sql เวอร์ชันเก่า (ก่อนมี sort_order)
-- ถ้าฐานข้อมูลใหม่ทั้งหมด ใช้ schema.sql เวอร์ชันล่าสุดได้เลย ไม่ต้องรันไฟล์นี้
-- หมายเหตุ: ถ้าคอลัมน์นี้มีอยู่แล้ว คำสั่งด้านล่างจะแจ้ง error "Duplicate column name" ซึ่งไม่เป็นอันตราย ข้ามได้เลย
-- ============================================================================

ALTER TABLE employees
    ADD COLUMN sort_order INT UNSIGNED NULL AFTER list_type;
